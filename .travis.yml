language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t stibulschi/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true stibulschi/docker-react npm run test